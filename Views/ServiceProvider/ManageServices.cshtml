@model ManageServicesViewModel
@{
    ViewData["Title"] = "Manage Services";
}

<section class="surface-card p-4 p-lg-5">
    <div class="page-title-row">
        <div>
            <h1 class="section-title">Service Person Manage On Services</h1>
            <p class="section-copy">After approval and login, service persons can add their service entries here.</p>
        </div>
    </div>

    <form asp-action="ManageServices" method="post" enctype="multipart/form-data" class="row g-3 mb-5">
        @Html.AntiForgeryToken()
        <div class="col-md-6">
            <label asp-for="ServiceName" class="form-label">Event Name</label>
            <input asp-for="ServiceName" class="form-control" />
            <span asp-validation-for="ServiceName" class="text-danger small"></span>
        </div>
        <div class="col-md-3">
            <label asp-for="ServiceCategory" class="form-label">Category</label>
            <select asp-for="ServiceCategory" class="form-select">
                <option>Party Plot</option>
                <option>Caterer</option>
                <option>Decoration</option>
                <option>Music</option>
                <option>Transporter</option>
            </select>
        </div>
        <div class="col-md-3">
            <label asp-for="EventTypeId" class="form-label">Event Type</label>
            <select asp-for="EventTypeId" class="form-select">
                @foreach (var eventType in Model.EventTypes)
                {
                    <option value="@eventType.Event_Id">@eventType.Event_Type</option>
                }
            </select>
        </div>
        <div class="col-md-3">
            <label asp-for="Price" class="form-label">Price</label>
            <input asp-for="Price" class="form-control" />
            <span asp-validation-for="Price" class="text-danger small"></span>
        </div>
        <div class="col-md-3">
            <label asp-for="PriceType" class="form-label">Price Type</label>
            <select asp-for="PriceType" class="form-select">
                <option value="For All">For All</option>
                <option value="100 Guest">100 Guest</option>
                <option value="Per Person">Per Person</option>
                <option value="Per Hour">Per Hour</option>
            </select>
        </div>
        <div class="col-md-6">
            <label asp-for="PhotoFile" class="form-label">Event Photo (Upload)</label>
            <input asp-for="PhotoFile" type="file" class="form-control" accept="image/*" />
        </div>
        <div class="col-12">
            <label asp-for="Description" class="form-label">Description</label>
            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
            <span asp-validation-for="Description" class="text-danger small"></span>
        </div>
        <div class="col-12">
            <button class="btn btn-brand" type="submit">Add Service</button>
        </div>
    </form>

    <div class="table-responsive">
        <table class="table table-modern">
            <thead><tr><th>Event Name</th><th>Category</th><th>Price</th><th>Price Type</th><th>Status</th></tr></thead>
            <tbody>
            @foreach (var service in Model.ExistingServices)
            {
                <tr>
                    <td>@service.Service_Name</td>
                    <td>@service.Service_Category</td>
                    <td>@service.Price.ToString("C0")</td>
                    <td>@service.Price_Type</td>
                    <td><span class="status-pill @(service.Is_Active ? "success" : "danger")">@(service.Is_Active ? "Active" : "Inactive")</span></td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</section>
